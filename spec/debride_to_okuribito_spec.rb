require 'spec_helper'

describe DebrideToOkuribito do
  it 'has a version number' do
    expect(DebrideToOkuribito::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
